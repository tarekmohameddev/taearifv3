# دليل تشخيص وإصلاح مشاكل GA4

## المشكلة الحالية
- ✅ Backend يعمل بشكل مثالي
- ❌ Frontend لا يرسل أحداث `page_view` إلى GA4
- ❌ GA4 لا يحتوي على بيانات للعرض

## الحلول المطبقة

### 1. تحسين تهيئة GA4
```typescript
// تم إضافة:
- انتظار تحميل الـ script قبل التكوين
- رسائل console للتحقق من التحميل
- معالجة الأخطاء
```

### 2. تحسين تتبع الأحداث
```typescript
// تم إضافة:
- فحص أن GA4 جاهز قبل إرسال الأحداث
- إعادة المحاولة التلقائية
- رسائل console للتتبع
```

### 3. تحسين GA4Provider
```typescript
// تم إضافة:
- state للتحقق من التهيئة
- تأخير إرسال الأحداث
- رسائل console للتتبع
```

## كيفية الاختبار

### 1. افتح Developer Tools
```bash
F12 → Console
```

### 2. ابحث عن الرسائل التالية:
```
🚀 GA4: Starting initialization with ID: G-WTN83NMVW1
✅ GA4: Script loaded successfully
✅ GA4: Configuration complete
📊 GA4: Tracking page view
🚀 GA4: Tracking page view
```

### 3. تحقق من Network Tab
- ابحث عن طلبات `googletagmanager.com`
- تأكد من أن الـ ID صحيح: `G-WTN83NMVW1`

### 4. تحقق من GA4 Real-time
- افتح Google Analytics
- اذهب إلى Real-time → Overview
- يجب أن تظهر مشاهدات الصفحات

## استكشاف الأخطاء

### إذا لم تظهر رسائل GA4:
1. تحقق من أن `NEXT_PUBLIC_GA4_ID` موجود في `.env.local`
2. أعد تشغيل الخادم: `npm run dev`
3. امسح cache المتصفح

### إذا ظهرت رسائل GA4 لكن لا توجد بيانات:
1. تحقق من أن GA4 ID صحيح
2. انتظر 24-48 ساعة للبيانات
3. تحقق من Real-time في GA4

### إذا ظهرت أخطاء:
1. تحقق من console للأخطاء
2. تأكد من أن الـ script يتم تحميله
3. تحقق من اتصال الإنترنت

## المتغيرات المطلوبة

أضف إلى `.env.local`:
```env
NEXT_PUBLIC_GA4_ID=G-WTN83NMVW1
NEXT_PUBLIC_GA4_LEGACY_ID=G-RVFKM2F9ZN
NEXT_PUBLIC_Backend_URL=http://localhost:3001
```

## النتيجة المتوقعة

بعد التطبيق:
- ✅ GA4 يتم تحميله بشكل صحيح
- ✅ أحداث page_view يتم إرسالها
- ✅ البيانات تظهر في GA4 Real-time
- ✅ Custom dimensions تعمل (tenant_id)
