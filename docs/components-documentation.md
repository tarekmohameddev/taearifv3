# توثيق المكونات - Live Editor

## نظرة عامة

هذا الدليل يوضح جميع المكونات المتاحة في Live Editor وكيفية استخدامها.

## فئات المكونات

### 1. مكونات التخطيط (Layout Components)

#### Hero Section
**الوصف:** قسم البطل الرئيسي للصفحة
**الاستخدام:** الصفحة الرئيسية، صفحات المنتجات
**الخصائص:**
- العنوان الرئيسي
- العنوان الفرعي
- وصف قصير
- زر العمل
- صورة خلفية

**مثال:**
```typescript
{
  title: "مرحباً بك في موقعنا",
  subtitle: "نقدم أفضل الخدمات",
  description: "نحن متخصصون في تقديم الحلول التقنية",
  buttonText: "ابدأ الآن",
  buttonLink: "/contact",
  backgroundImage: "/images/hero-bg.jpg"
}
```

#### Header
**الوصف:** رأس الصفحة مع القائمة
**الاستخدام:** جميع الصفحات
**الخصائص:**
- الشعار
- قائمة التنقل
- معلومات التواصل
- أزرار العمل

**مثال:**
```typescript
{
  logo: "/images/logo.png",
  navigation: [
    { label: "الرئيسية", link: "/" },
    { label: "من نحن", link: "/about" },
    { label: "الخدمات", link: "/services" },
    { label: "اتصل بنا", link: "/contact" }
  ],
  contactInfo: {
    phone: "+966501234567",
    email: "info@example.com"
  }
}
```

#### Footer
**الوصف:** تذييل الصفحة
**الاستخدام:** جميع الصفحات
**الخصائص:**
- معلومات الشركة
- روابط مفيدة
- وسائل التواصل الاجتماعي
- معلومات قانونية

**مثال:**
```typescript
{
  companyInfo: {
    name: "اسم الشركة",
    description: "وصف مختصر للشركة",
    address: "العنوان الكامل"
  },
  quickLinks: [
    { label: "الرئيسية", link: "/" },
    { label: "من نحن", link: "/about" }
  ],
  socialMedia: {
    facebook: "https://facebook.com/company",
    twitter: "https://twitter.com/company",
    instagram: "https://instagram.com/company"
  }
}
```

### 2. مكونات المحتوى (Content Components)

#### Half Text Half Image
**الوصف:** نص وصورة جنباً إلى جنب
**الاستخدام:** صفحات الخدمات، من نحن
**الخصائص:**
- العنوان
- النص الوصفي
- الصورة
- ترتيب المحتوى (نص-صورة أو صورة-نص)

**مثال:**
```typescript
{
  title: "عن خدماتنا",
  content: "نقدم خدمات متخصصة في مجال التكنولوجيا...",
  image: "/images/service-image.jpg",
  layout: "text-image", // أو "image-text"
  buttonText: "اعرف المزيد",
  buttonLink: "/services"
}
```

#### Testimonials
**الوصف:** شهادات العملاء
**الاستخدام:** الصفحة الرئيسية، صفحة الخدمات
**الخصائص:**
- اسم العميل
- صورة العميل
- الشهادة
- التقييم
- المسمى الوظيفي

**مثال:**
```typescript
{
  testimonials: [
    {
      name: "أحمد محمد",
      position: "مدير شركة",
      image: "/images/client1.jpg",
      content: "خدمة ممتازة وسريعة",
      rating: 5
    }
  ]
}
```

#### Why Choose Us
**الوصف:** لماذا تختارنا
**الاستخدام:** الصفحة الرئيسية، صفحة الخدمات
**الخصائص:**
- العنوان الرئيسي
- قائمة المميزات
- الأيقونات
- الألوان

**مثال:**
```typescript
{
  title: "لماذا تختارنا؟",
  features: [
    {
      icon: "🚀",
      title: "سرعة في التنفيذ",
      description: "ننفذ مشاريعك بسرعة فائقة"
    },
    {
      icon: "💎",
      title: "جودة عالية",
      description: "نقدم أعلى مستويات الجودة"
    }
  ]
}
```

### 3. مكونات العقارات (Property Components)

#### Property Slider
**الوصف:** عرض العقارات في شكل slider
**الاستخدام:** الصفحة الرئيسية، صفحة العقارات
**الخصائص:**
- قائمة العقارات
- صور العقارات
- معلومات العقار
- أزرار العمل

**مثال:**
```typescript
{
  title: "عقارات مميزة",
  properties: [
    {
      id: "prop1",
      title: "فيلا فاخرة",
      price: "2,500,000 ريال",
      location: "الرياض",
      image: "/images/property1.jpg",
      features: ["4 غرف", "3 حمامات", "حديقة"]
    }
  ]
}
```

#### Property Filter
**الوصف:** فلترة العقارات
**الاستخدام:** صفحة العقارات
**الخصائص:**
- فلتر الموقع
- فلتر السعر
- فلتر النوع
- فلتر المساحة

**مثال:**
```typescript
{
  filters: {
    location: ["الرياض", "جدة", "الدمام"],
    priceRange: {
      min: 500000,
      max: 5000000
    },
    propertyType: ["فيلا", "شقة", "أرض"],
    area: {
      min: 100,
      max: 1000
    }
  }
}
```

#### Map Section
**الوصف:** خريطة العقارات
**الاستخدام:** صفحة العقارات
**الخصائص:**
- إحداثيات العقارات
- معلومات العقار
- أزرار التنقل
- تخصيص الخريطة

**مثال:**
```typescript
{
  center: {
    lat: 24.7136,
    lng: 46.6753
  },
  zoom: 10,
  properties: [
    {
      id: "prop1",
      lat: 24.7136,
      lng: 46.6753,
      title: "عقار مميز",
      price: "1,500,000 ريال"
    }
  ]
}
```

### 4. مكونات التواصل (Contact Components)

#### Contact Form
**الوصف:** نموذج التواصل
**الاستخدام:** صفحة اتصل بنا
**الخصائص:**
- حقول النموذج
- التحقق من صحة البيانات
- رسائل النجاح/الخطأ
- إعدادات الإرسال

**مثال:**
```typescript
{
  fields: [
    {
      name: "name",
      label: "الاسم",
      type: "text",
      required: true
    },
    {
      name: "email",
      label: "البريد الإلكتروني",
      type: "email",
      required: true
    },
    {
      name: "message",
      label: "الرسالة",
      type: "textarea",
      required: true
    }
  ],
  submitText: "إرسال الرسالة",
  successMessage: "تم إرسال الرسالة بنجاح"
}
```

#### Contact Cards
**الوصف:** بطاقات معلومات التواصل
**الاستخدام:** صفحة اتصل بنا
**الخصائص:**
- العنوان
- الهاتف
- البريد الإلكتروني
- الموقع
- ساعات العمل

**مثال:**
```typescript
{
  cards: [
    {
      title: "المكتب الرئيسي",
      address: "الرياض، المملكة العربية السعودية",
      phone: "+966501234567",
      email: "info@example.com",
      hours: "الأحد - الخميس: 8:00 - 17:00"
    }
  ]
}
```

## إعدادات المكونات

### الألوان
```typescript
interface ColorSettings {
  primary: string;      // اللون الأساسي
  secondary: string;    // اللون الثانوي
  accent: string;       // لون التمييز
  background: string;    // لون الخلفية
  text: string;         // لون النص
}
```

### الخطوط
```typescript
interface FontSettings {
  primary: string;      // الخط الأساسي
  secondary: string;    // الخط الثانوي
  sizes: {
    h1: string;         // حجم العنوان الرئيسي
    h2: string;         // حجم العنوان الفرعي
    body: string;       // حجم النص العادي
  };
}
```

### المسافات
```typescript
interface SpacingSettings {
  padding: {
    top: string;        // المسافة العلوية
    bottom: string;     // المسافة السفلية
    left: string;       // المسافة اليسرى
    right: string;      // المسافة اليمنى
  };
  margin: {
    top: string;        // الهامش العلوي
    bottom: string;     // الهامش السفلي
    left: string;       // الهامش الأيسر
    right: string;      // الهامش الأيمن
  };
}
```

## استخدام المكونات

### إضافة مكون
```typescript
// إضافة مكون جديد
const newComponent = {
  id: generateId(),
  type: 'hero',
  name: 'Hero Section',
  componentName: 'hero1',
  data: {
    title: 'عنوان جديد',
    subtitle: 'عنوان فرعي',
    // ... other properties
  },
  position: 0
};
```

### تحديث مكون
```typescript
// تحديث مكون موجود
const updatedComponent = {
  ...existingComponent,
  data: {
    ...existingComponent.data,
    title: 'عنوان محدث'
  }
};
```

### حذف مكون
```typescript
// حذف مكون
const filteredComponents = components.filter(
  component => component.id !== componentIdToDelete
);
```

## أفضل الممارسات

### 1. تسمية المكونات
- استخدم أسماء واضحة ومفهومة
- تجنب الأسماء الطويلة
- استخدم اللغة العربية

### 2. تنظيم البيانات
- اجمع البيانات المتشابهة
- استخدم هياكل ثابتة
- تجنب البيانات المكررة

### 3. الأداء
- استخدم الصور المضغوطة
- تجنب المكونات الثقيلة
- اختبر سرعة التحميل

### 4. التصميم
- حافظ على التناسق
- استخدم الألوان المناسبة
- اجعل التصميم متجاوب

---

**تاريخ التحديث:** ${new Date().toLocaleDateString('ar-SA')}
**الإصدار:** 1.0.0
