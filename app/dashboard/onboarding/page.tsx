import { OnboardingPage } from "@/components/onboarding/OnboardingPage";

export const metadata = {
  title: "إعداد الموقع — تعاريف",
};

export default function Page() {
  return <OnboardingPage />;
}
